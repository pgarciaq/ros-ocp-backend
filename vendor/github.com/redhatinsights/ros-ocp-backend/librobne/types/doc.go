// Package types holds canonical digest and recommendation types shared by every
// librobne consumer. Scan or parse directly into DigestRow, KeyedDigest, and
// ContainerRec. Do not convert from product models. RateCard is deposited by
// the product; an empty card does not invent "USD".
package types
