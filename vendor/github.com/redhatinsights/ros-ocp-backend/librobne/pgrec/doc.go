package pgrec

// Package pgrec upserts native recommendation rows into PostgreSQL (containers
// and shipped 2b entities). Processor and the robne CLI import it;
// robne-operator must not (ADR-0305).
