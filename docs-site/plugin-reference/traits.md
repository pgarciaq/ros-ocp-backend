# Plugin traits (moved)

This page moved to **Architecture → [Plugin Traits](../architecture/plugin-traits.md)** (after Plugin Architecture, before Plugin Execution Phases).

Update bookmarks to `/architecture/plugin-traits/`.
